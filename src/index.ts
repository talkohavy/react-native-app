// The "main" property in the "package.json" file is pointing to this file as the main entry point.
import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
